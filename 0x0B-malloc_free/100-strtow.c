#include <stdlib.h>
/**
 * strtow - return a pointer to an array of strings (words)
 * @str: string to turn to words
 *
 * ReturnL pointer to array of strings
 */
char **strtow(__attribute__((unused)) char *str)
{
	
	return (0);
}
